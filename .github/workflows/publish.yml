name: Publish to npm

# Disabled — wunderland-sol is not an npm package.
# The publishable CLI package lives in packages/wunderland/ and is
# published from the parent monorepo.
on:
  workflow_dispatch:

jobs:
  publish:
    runs-on: ubuntu-latest
    if: false
    steps:
      - run: echo "Disabled"
