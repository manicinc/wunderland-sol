#!/usr/bin/env sh
. "$(dirname -- "$0")/_/husky.sh"

echo "🔍 Running Frame Codex pre-commit checks..."

# Run auto-indexing with validation
npm run index -- --validate

# Check exit code
if [ $? -ne 0 ]; then
  echo "❌ Pre-commit validation failed. Please fix errors before committing."
  exit 1
fi

echo "✅ Pre-commit checks passed!"

