# pnpm configuration
shamefully-hoist=true
strict-peer-dependencies=false

# Force flat node_modules for Electron compatibility
node-linker=hoisted

# Ensure electron is properly hoisted for Electron main process
public-hoist-pattern[]=electron
public-hoist-pattern[]=electron-*

