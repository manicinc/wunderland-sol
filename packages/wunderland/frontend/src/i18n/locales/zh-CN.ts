import en from './en';

/**
 * Temporary placeholder for Chinese (Simplified) translations.
 * The previous file became corrupt, so we fall back to English strings
 * to keep the build working until proper translations are restored.
 */
export default en;
