# =============================================================================
# Frontend (Vite) — Environment Configuration
# =============================================================================
# Copy this file to `frontend/.env.local` for local development.
# NOTE: `VITE_` variables are exposed to the browser.
# =============================================================================

# Backend API base URL (used by `frontend/src/utils/api.ts`)
VITE_API_BASE_URL=http://localhost:3001/api

# AgentOS routing toggles (optional)
VITE_AGENTOS_ENABLED=false
VITE_AGENTOS_CLIENT_MODE=proxy
VITE_AGENTOS_CHAT_PATH=/agentos/chat
VITE_AGENTOS_STREAM_PATH=/agentos/stream

# Supabase (optional)
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

# Misc (optional)
VITE_SALES_EMAIL=team@voicechatassistant.com
VITE_APP_VERSION=0.0.0

