{
  "name": "@gitpaywidget/payments-core",
  "version": "0.1.0-alpha.0",
  "private": true,
  "description": "Provider-agnostic payment abstraction used by GitPayWidget backend.",
  "type": "module",
  "sideEffects": false,
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "clean": "rimraf dist tsconfig.tsbuildinfo",
    "build": "pnpm run clean && tsc -p tsconfig.json && node ../../.github/scripts/fix-esm-imports.js dist"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.6.3",
    "rimraf": "^6.0.1"
  },
  "dependencies": {
    "stripe": "^19.3.1"
  }
}
