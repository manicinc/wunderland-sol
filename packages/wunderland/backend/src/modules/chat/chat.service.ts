/**
 * @file chat.service.ts
 * @description Placeholder injectable service for the ChatModule. Business
 * logic currently lives in the Express route handlers
 * (`features/chat/chat.routes.ts`). This service will be the target for
 * incremental extraction of that logic into a proper NestJS service layer.
 */

import { Injectable } from '@nestjs/common';

@Injectable()
export class ChatService {}
