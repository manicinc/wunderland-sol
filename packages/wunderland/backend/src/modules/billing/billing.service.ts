/**
 * @file billing.service.ts
 * @description Placeholder injectable service for the BillingModule. Business
 * logic currently lives in the Express route handlers and the
 * `lemonsqueezy.service.ts` / `checkout.repository.ts` files. This service
 * will be the target for incremental extraction into a proper NestJS
 * service layer.
 */

import { Injectable } from '@nestjs/common';

@Injectable()
export class BillingService {}
