import type { Request, Response } from 'express';

/**
 * Type declarations for the legacy JS prompt routes module (`prompt.routes.js`).
 */
export function GET(req: Request, res: Response): Promise<void>;

